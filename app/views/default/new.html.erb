<h1>New <%=current_model.name%></h1>

<% form_for(current_object) do |f| %>
  <%= f.error_messages %>

  <%=get_content_for('fields')%>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', url_for(:action => 'index') %>
